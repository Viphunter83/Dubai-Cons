# 🏗️ Архитектура Dubai Cons AI Suite

**Дата:** 27 октября 2025  
**Статус:** Текущая архитектура

---

## 📁 СТРУКТУРА ПРОЕКТА

Проект состоит из **ОДНОГО Frontend** и **ОДНОГО Backend**:

```
Dubai Cons/
├── src/                    # BACKEND (Python + FastAPI)
│   ├── main.py            # FastAPI приложение
│   ├── api/routes/       # API endpoints (21 штук)
│   ├── database/         # SQLite база данных
│   ├── ai_modules/       # AI интеграция
│   └── services/         # Бизнес логика
│
└── frontend/             # FRONTEND (React + Vite) ⭐
    ├── src/
    │   ├── App.jsx       # Главный компонент
    │   ├── pages/       # 4 страницы
    │   └── components/   # Компоненты
    └── package.json      # npm зависимости
```

---

## ✅ ТЕКУЩАЯ АРХИТЕКТУРА

### Backend (Python + FastAPI)
**URL:** http://localhost:8000  
**Технологии:**
- Python 3.10
- FastAPI
- SQLAlchemy
- SQLite

**Функции:**
- REST API (21 endpoints)
- AI генерация дизайна
- 3D визуализация
- CRUD операции
- Stats API

### Frontend (React + Vite)
**URL:** http://localhost:3000  
**Технологии:**
- React 18
- Vite 5
- React Router
- Axios

**Функции:**
- 4 страницы (Home, Projects, Clients, Design)
- API интеграция
- UI/UX интерфейс

---

## 🤔 ЧТО ОЗНАЧАЕТ "ДВА ФРОНТЕНДА"?

Вы упомянули "два фронтенда", но у нас есть **ТОЛЬКО ОДИН Frontend**:

### Один Frontend (React + Vite)
```
frontend/
├── src/
│   ├── pages/
│   │   ├── Home.jsx       # Главная страница
│   │   ├── Projects.jsx   # Проекты
│   │   ├── Clients.jsx   # Клиенты
│   │   └── Design.jsx     # AI дизайн
│   └── App.jsx           # Роутинг
```

**Это стандартная React архитектура** с Vite в качестве bundler.

---

## 🚀 КАК ЭТО РАБОТАЕТ

### Архитектура "Client-Server":

```
┌─────────────┐         HTTP/REST         ┌─────────────┐
│   Frontend  │ ──────────────────────────> │   Backend   │
│ (React+Vite)│ <────────────────────────── │  (FastAPI)  │
│             │      JSON Responses        │             │
└─────────────┘                             └─────────────┘
     :3000                                          :8000
```

**Flow:**
1. Пользователь открывает браузер → Frontend (React)
2. Frontend делает запросы к Backend API
3. Backend обрабатывает запросы и возвращает данные
4. Frontend отображает данные пользователю

---

## 📊 ТЕКУЩАЯ СТАТИСТИКА

### Что запущено:

**Backend:**
- ✅ Сервер: http://localhost:8000
- ✅ Swagger: http://localhost:8000/docs
- ✅ API endpoints: 21

**Frontend:**
- ✅ React app: http://localhost:3000
- ✅ Vite dev server: работает
- ✅ Pages: 4 страницы готовы

---

## ✅ ВСЁ ПРАВИЛЬНО!

У вас **один Frontend** на React + Vite, который:
- Отображает UI пользователю
- Связывается с Backend через REST API
- Использует React Router для навигации

Это стандартная современная архитектура! ✅

---

**Версия:** 1.0  
**Дата:** 27 октября 2025  
**Статус:** Актуальная архитектура

